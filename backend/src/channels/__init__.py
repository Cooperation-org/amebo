# Channel abstraction layer
#
# Normalizes all communication channels (Slack, web, CLI, future channels)
# into a common contract so the agent core never touches channel-specific APIs.
#
# See docs/CHANNEL_CONTRACT.md for design rationale.
