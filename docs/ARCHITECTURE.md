# System Architecture

> **Legacy framing.** This document describes an earlier "workspace intelligence platform"
> shape (ChromaDB vector store, JWT/password auth, document-upload Q&A). The canonical model
> for Amebo is now the **agency layer** described in [`BOUNDARIES.md`](BOUNDARIES.md) and
> [`../OVERVIEW.md`](../OVERVIEW.md): one engine (live + claw), pgvector via Abra, OAuth/SSO.
> Parts of this file are superseded. Read `BOUNDARIES.md` first; treat anything here that
> conflicts with it as historical, not current guidance.

🏗️ **Amebo Architecture** - Comprehensive overview of system design, components, and data flow.

## 🎯 Architecture Overview

Slack Helper Bot follows a modern, scalable architecture with clear separation of concerns, multi-tenant security, and AI-powered intelligence.

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                 USER LAYER                                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│  Web Dashboard (Next.js)     │     Slack Interface     │    Mobile (Future)    │
│  • Q&A Interface             │     • /ask commands     │    • React Native     │
│  • Workspace Management      │     • /askall commands  │    • Progressive Web  │
│  • Document Upload           │     • Bot responses     │    • App (PWA)        │
│  • Team Management           │     • Real-time sync    │                       │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              API GATEWAY LAYER                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│                            FastAPI Application                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                │
│  │  Auth Routes    │  │ Workspace Routes│  │ Document Routes │                │
│  │  • Login/Signup │  │ • CRUD Ops      │  │ • Upload        │                │
│  │  • JWT Tokens   │  │ • Backfill      │  │ • Processing    │                │
│  │  • User Mgmt    │  │ • Sync Status   │  │ • Search Index  │                │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘                │
│                                                                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                │
│  │   Q&A Routes    │  │   Team Routes   │  │  Slack Routes   │                │
│  │  • Ask Question │  │ • Invitations   │  │ • Webhooks      │                │
│  │  • Get History  │  │ • Role Mgmt     │  │ • Commands      │                │
│  │  • Confidence   │  │ • Permissions   │  │ • Event Handler │                │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘                │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                             SERVICE LAYER                                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                │
│  │   QA Service    │  │ Document Service│  │  Slack Service  │                │
│  │  • Claude AI    │  │ • Text Extract  │  │ • API Client    │                │
│  │  • Vector Search│  │ • Chunking      │  │ • Message Sync  │                │
│  │  • Context Agg  │  │ • Indexing      │  │ • Channel Mgmt  │                │
│  │  • Response Gen │  │ • File Handling │  │ • User Mapping  │                │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘                │
│                                                                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                │
│  │ Backfill Service│  │  Email Service  │  │  Auth Service   │                │
│  │  • Scheduled    │  │ • SMTP Client   │  │ • JWT Handling  │                │
│  │  • Historical   │  │ • Templates     │  │ • Password Hash │                │
│  │  • Incremental  │  │ • Invitations   │  │ • Session Mgmt  │                │
│  │  • Rate Limiting│  │ • Notifications │  │ • Multi-tenant  │                │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘                │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              DATA LAYER                                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                │
│  │   PostgreSQL    │  │    ChromaDB     │  │  File Storage   │                │
│  │  • User Data    │  │ • Vector Embeds │  │ • Documents     │                │
│  │  • Workspaces   │  │ • Similarity    │  │ • Attachments   │                │
│  │  • Messages     │  │ • Collections   │  │ • Temp Files    │                │
│  │  • Documents    │  │ • Metadata      │  │ • Logs          │                │
│  │  • Teams        │  │ • Search Index  │  │                 │                │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘                │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 🔐 Multi-Tenant Security Architecture

### 4-Layer Isolation Model

```
┌─────────────────────────────────────────────────────────────────┐
│                    ORGANIZATION LAYER                           │
├─────────────────────────────────────────────────────────────────┤
│  org_id: 1          │  org_id: 2          │  org_id: 3        │
│  ┌─────────────────┐ │ ┌─────────────────┐ │ ┌───────────────┐ │
│  │   Company A     │ │ │   Company B     │ │ │   Company C   │ │
│  │  • Users        │ │ │  • Users        │ │ │  • Users      │ │
│  │  • Workspaces   │ │ │  • Workspaces   │ │ │  • Workspaces │ │
│  │  • Documents    │ │ │  • Documents    │ │ │  • Documents  │ │
│  └─────────────────┘ │ └─────────────────┘ │ └───────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 🧠 AI & Knowledge Processing Pipeline

### Document Processing Flow

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   File Upload   │───▶│  Text Extract   │───▶│   Chunking      │
│                 │    │                 │    │                 │
│ • PDF, DOCX     │    │ • PyPDF2        │    │ • Semantic      │
│ • TXT, MD       │    │ • python-docx   │    │ • Size Limits   │
│ • Drag & Drop   │    │ • Plain Text    │    │ • Overlap       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                        │
                                                        ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   ChromaDB      │◄───│   Embeddings    │◄───│   Metadata      │
│                 │    │                 │    │                 │
│ • Vector Store  │    │ • Claude Embeds │    │ • Workspace ID  │
│ • Collections   │    │ • Similarity    │    │ • File Info     │
│ • Search Index  │    │ • Cosine Dist   │    │ • Timestamps    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 📊 Database Schema Design

### Core Tables

```sql
-- Organizations (Multi-tenancy)
CREATE TABLE organizations (
    org_id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Users (Authentication & Authorization)
CREATE TABLE users (
    user_id SERIAL PRIMARY KEY,
    org_id INTEGER REFERENCES organizations(org_id),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(255),
    role VARCHAR(50) DEFAULT 'member',
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Workspaces (Slack Integration)
CREATE TABLE workspaces (
    workspace_id VARCHAR(50) PRIMARY KEY,
    org_id INTEGER REFERENCES organizations(org_id),
    team_name VARCHAR(255) NOT NULL,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Documents (File Management)
CREATE TABLE documents (
    document_id SERIAL PRIMARY KEY,
    org_id INTEGER REFERENCES organizations(org_id),
    workspace_id VARCHAR(50) REFERENCES workspaces(workspace_id),
    file_name TEXT NOT NULL,
    file_type VARCHAR(100),
    chromadb_collection VARCHAR(255),
    chunk_count INTEGER DEFAULT 0,
    uploaded_by INTEGER REFERENCES users(user_id),
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW()
);
```

## 🔄 Data Flow Patterns

### Q&A Query Flow

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   User      │───▶│  Vector     │───▶│  Context    │───▶│   Claude    │
│  Question   │    │  Search     │    │ Aggregation │    │  AI Gen     │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

## Scalability Considerations

### Horizontal Scaling

```
┌─────────────────────────────────────────────────────────────────┐
│                        LOAD BALANCER                            │
└─────────────────────────────────────────────────────────────────┘
                                │
                ┌───────────────┼───────────────┐
                ▼               ▼               ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   API Server 1  │  │   API Server 2  │  │   API Server N  │
│                 │  │                 │  │                 │
│ • FastAPI       │  │ • FastAPI       │  │ • FastAPI       │
│ • Stateless     │  │ • Stateless     │  │ • Stateless     │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

## 🔒 Security Architecture

### Authentication & Authorization

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   JWT Token     │───▶│  Middleware     │───▶│  Route Guard    │
│                 │    │                 │    │                 │
│ • HS256 Signed  │    │ • Token Verify  │    │ • Role Check    │
│ • 30min Expiry  │    │ • User Extract  │    │ • Org Isolation │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

This architecture provides a solid foundation for a scalable, secure, and maintainable AI-powered workspace intelligence platform.